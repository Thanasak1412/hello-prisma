const rootUser = "/users";

export const PATH_EXTERNAL_API = {
  getUsers: "/users",
  createUser: "/users",
};

export const PATH_API = {
  getUsers: rootUser,
  createUser: `${rootUser}/create`,
};
